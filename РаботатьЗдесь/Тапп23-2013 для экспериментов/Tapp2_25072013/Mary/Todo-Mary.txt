Файл ТОДО для проекта Tapp23-Mary 

8 января 2020г
- эксперимент по внедрению сменных адаптеров БД.
  + сделал бекап проекта, чтобы потом вернуться если неудачно получится.
  + завел проекты адаптеров БД и распихал в них файлы классов адаптеров.
  + выставил зависимости от Mary правильно.
  + для аксцесса - перенес файл БД из ресурсов Mary в ресурсы MsJetDbAdapter.
    + и исправил код извлечения этого файла.
  - надо написать код загрузки сборки адаптера: функция MDbAdapterBase.loadDbAdapterFromAssembly(string assemblyAndClassName)  
  - Ошибки: The property or indexer 'Mary.MCell.ModiTime' cannot be used in this context because the set accessor is inaccessible	V:\МоиПроекты\ДвижокТапп\РаботатьЗдесь\Тапп23-2013 для экспериментов\Tapp2_25072013\MsSql2005DbAdapter\MsSql2005DbAdapter.cs
    Это я проперти set установил в internal, чтобы пользователь движка не мог их изменить, а код мог, так как он внутри сборки.
    А теперь код тоже вне сборки, и это удачное решение перестало работать.
    Надо что-то придумать, чтобы и пользователь мог только читать проперти, и код мог изменять значение извне сборки.
    А) можно убрать set из проперти, а вместо нее использовать функцию, которую скрыть из документации, так как она только для служебного использования.
      И тогда весь код Движка переделать на эту функцию вместо проперти.set
      Это 3 таких функции надо - по числу таких проперти.
      - MCell.CreaTime
      - MCell.ModiTime
      - MCell.CellMode
      Но они наследуются производными классами, так что надо это наследование отследить.
    Б) можно сделать специальную функцию, в которую все значения переменных одним вызовом передавать.
      Это только для процессов загрузки ячеек из БД, чтобы не использовать проперти и их триггерные условия вообще.
      + я сделал конструктор в MCellB для этого. А больше оно нигде не вызывается сейчас.
    + я сделал оба этих решения, посмотрю что пригодится, но конструктор - удобнее.
      TODO: Если аксессоры (А) не пригодятся, их надо удалить из кода.    
  - затем надо проверить работу функции private static MDbAdapterBase loadDbAdapterFromAssembly(string assemblyAndClassName,  Object[] args)
    Я ее написал, но не уверен, что это работает правильно.
    И что потом этот адаптер будет правильно работать...    
    - исправил, работает, сборку адаптера Аксцесса загружает.
- имеющиеся тесты прошли успешно, ячейки и связи создаются, однако есть странные глюки 
   с приложением TestShell - хотя код обычный, но с движком это приложение работает плохо - выбрасывает исключения неожиданные.
   Это как-то связано с COM, но что и как - непонятно.
  - консольное приложение работает нормально, а винформс - глючит. 
- приложение TestShell работоспособно, хотя оно устарело и недоделано.
  И ненадежно, я не уверен что оно не испортит Структуру Сущностей.
  Но это единственный имеющийся браузер по Солюшену.
  Поэтому его надо все же развивать. Надо запланировать работы по нему именно.
  Я хочу иметь браузер Солюшена с возможностью просматривать и править структуру ячеек и связей.
Выводы на сегодня:
 + движок переделан и опробован, в целом успешно.
 + Движок работает с адаптером БД Аксцесс - успешно, хотя возможны проблемы в дальнейшем.
 + Реализация загружаемых адаптеров БД создана и опробована успешно.
   - хотя могут быть невыявленные проблемы.       
 + Инструкция по созданию адаптеров БД написана.
 
    